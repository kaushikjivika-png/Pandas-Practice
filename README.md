# 📊 Pandas Practice

This repository documents my journey of learning and practicing **Pandas**, a powerful Python library used for data manipulation and analysis.

The repository is organized from **basic to advanced concepts** and includes hands-on practice to build strong practical skills for data analysis.

## 🛠️ Technologies Used

* Python
* Pandas
* Jupyter Notebook / VS Code

## 📚 Topics Covered

### 01. Pandas Basics

* Introduction to Pandas
* Installing and importing Pandas
* Creating Series and DataFrames
* Rows, columns, index, and values

### 02. Data Loading and Exploration

* `read_csv()`
* `read_excel()`

### 03. Selection and Indexing

* Selecting single and multiple columns
* Row selection
* `loc[]`
* `iloc[]`
* Boolean indexing

### 04. Filtering Data

* Single condition filtering
* Multiple conditions
* AND (`&`)
* OR (`|`)
* NOT (`~`)
* `isin()`
* `between()`

### 05. Data Cleaning

* Renaming columns
* Removing duplicates
* `duplicated()`
* `drop_duplicates()`
* Changing data types
* `astype()`
* Cleaning inconsistent values

### 06. Missing Values

* `isnull()`
* `notnull()`
* `dropna()`
* `fillna()`
* Forward fill
* Backward fill
* Mean, median, and mode imputation

### 07. Sorting Data

* `sort_values()`
* `sort_index()`
* Sorting by multiple columns

### 08. String Operations

* `str.lower()`
* `str.upper()`
* `str.strip()`
* `str.replace()`
* `str.contains()`
* `str.startswith()`
* `str.endswith()`
* `str.split()`
* `str.len()`

### 09. GroupBy and Aggregation

* `groupby()`
* `sum()`
* `mean()`
* `median()`
* `min()`
* `max()`
* `count()`
* `size()`
* `agg()`
* Multiple aggregations

### 10. Combining DataFrames

* `concat()`
* `merge()`
* Inner Join
* Left Join
* Right Join
* Outer Join
* Joining on multiple columns

### 11. Reshaping Data

* `pivot()`
* `pivot_table()`
* `melt()`
* `stack()`
* `unstack()`

### 12. Date and Time

* `pd.to_datetime()`
* Extracting year, month, and day
* Date filtering
* Timestamp operations
* `.dt` accessor

### 13. Advanced Operations

* `apply()`
* `map()`
* `transform()`
* Lambda functions
* Custom functions
* Method chaining

### 14. Window Operations

* `rolling()`
* Moving averages
* `expanding()`
* Cumulative operations
* `rank()`

### 15. Practice and Interview Preparation

* Topic-wise practice questions
* Data cleaning exercises
* Data manipulation problems
* Interview-style questions
* Real-world datasets

## 📁 Repository Structure

```text
Pandas-Practice/
│
├── 01_Basics/
├── 02_Data_Exploration/
├── 03_Selection_and_Indexing/
├── 04_Filtering/
├── 05_Data_Cleaning/
├── 06_Missing_Values/
├── 07_Sorting/
├── 08_String_Operations/
├── 09_GroupBy_and_Aggregation/
├── 10_Merging_and_Joining/
├── 11_Reshaping_Data/
├── 12_Date_and_Time/
├── 13_Advanced_Operations/
├── 14_Window_Operations/
├── 15_Mini_Project/
├── datasets/
└── README.md
```

## 🎯 Goal

The goal of this repository is to build strong practical skills in Pandas.

This repository will be continuously updated as I learn, practice, and explore more advanced concepts in Pandas.
